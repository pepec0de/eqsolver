#ifndef STRINGUTILS_H
#define STRINGUTILS_H

class stringutils {
    public:
        /*
         * stringutils();
         * virtual ~stringutils();
         *
         */
        vector<string> splitString(string str, char deliminator);
    protected:
        
    private:

};

#endif // STRINGUTILS_H
